name: Greeting workflow
on:
  - push
  - pull_request
  - workflow_dispatch
jobs:
  build:
    # Job name is Greeting
    name: Swift test
    # This job runs on Linux
    runs-on: windows-latest
    steps:
      - run: swift --version
