name: Greeting workflow
on:
  - push
  - pull_request
  - workflow_dispatch
jobs:
  dockerfiletest:
    runs-on: ubuntu-latest
    steps:
      - uses: nohomers/no-homers-hello-world-docker-action@entrypoint-injection-test
        with:
          who-to-greet: Lucas the awesome


  
