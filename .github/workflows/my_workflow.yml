name: Greeting workflow
on:
  - push
  - pull_request
  - workflow_dispatch
jobs:
  my_job:
    runs-on: ubuntu-linux
    container:
      image: node:14.16
      env:
        NODE_ENV: development
      ports:
        - 80
      volumes:
        - my_docker_volume:/volume_mount
      options: --cpus 1
      
    steps:
      - run: echo "test"
