name: Greeting workflow
on:
  - push
  - pull_request
  - workflow_dispatch
jobs:
  build:
    # Job name is Greeting
    name: Greeting
    # This job runs on Linux
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - run: exit 1
      - run: echo "this should never work!"
      
