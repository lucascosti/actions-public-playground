name: Context testing
on:
  - push
  - pull_request
  - workflow_dispatch
jobs:
  one:
    runs-on: ubuntu-latest
    steps:
      - name: Dump GitHub context
        run: echo "${{ toJSON(github) }}"
