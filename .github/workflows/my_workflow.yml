name: Greeting workflow
on:
  - push
  - pull_request
  - workflow_dispatch
jobs:
  powershell:
    # Job name is Greeting
    name: Powershell
    runs-on: windows-latest
    steps:
      - run: echo "hello"
      - run: echo "::notice ::This is powershell!"
      - run: echo "bye!"
    
