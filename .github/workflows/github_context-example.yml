name: Step condition
on: push
jobs:
  ci-job:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1
        id: step1
        run: echo step 1
      - name: Step 2
        id: step2
        run: echo step 2
      - name: dump context
        run: echo '${{ toJSON(steps) }}' 
