name: Build
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build with set
        run: ls ${{ runner.temp }}
      - name: Upload logs on fail
        if: ${{ failure }}
        uses: actions/upload-artifact@v2
        with:
          name: Build failure logs
          path: ${{ runner.temp }}
