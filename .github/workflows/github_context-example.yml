name: Redis service container
on: push
jobs:
  redis-job:
    runs-on: ubuntu-latest
    services:
      redis:
        image: redis
        ports:
          - 6379:6379
    steps:
      - run: echo '${{ toJSON(job) }}'
      - run: 'hello'
      - run: return 1
      - run: 'hello2'
      - run: echo '${{ toJSON(job) }}' 
