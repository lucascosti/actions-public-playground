name: Run CI
on: push

env:
  list_command: ls
  super_duper_var: "it's a secret"
  
jobs:
  linux_job:
    runs-on: ubuntu-latest
    steps:
      - run: ${{ env.list_command }}
      - name: Dump env context
        run: echo '${{ toJSON(steps) }}'

  windows_job:
    runs-on: windows-latest
    env:
      list_command: dir
    steps:
      - run: ${{ env.list_command }}
      - name: Dump env context
        run: echo '${{ toJSON(env) }}'
