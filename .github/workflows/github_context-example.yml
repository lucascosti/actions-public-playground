name: Build
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build with set
        run: |
          echo Hi Mum
          echo "::set-output name=test::hi!"
  
  debug:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Dump needs context
        run: echo '${{ toJSON(needs) }}'
